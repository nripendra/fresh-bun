{
  "name": "fresh-bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "bunx @biomejs/biome check --write ./packages",
    "format": "bunx @biomejs/biome format --write ./packages"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}
