{
  "name": "fresh-bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "bunx @biomejs/biome check ./packages",
    "format": "bunx @biomejs/biome format --write ./packages",
    "lint:fix": "bunx @biomejs/biome check --write ./packages"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}
